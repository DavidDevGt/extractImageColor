{
  "name": "chromagrid",
  "version": "1.0.0",
  "description": "ChromaGrid es una webapp para extraer la paleta de colores de las imagenes",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsc -w & vite",
    "build": "rimraf dist && vite build",
    "start": "vite preview"
  },
  "devDependencies": {
    "@types/node-polyglot": "^2.5.0",
    "rimraf": "^6.0.1",
    "typescript": "^4.9.4",
    "vite": "^4.0.0"
  },
  "dependencies": {
    "node-polyglot": "^2.6.0"
  }
}
