{
  "name": "extractimagecolor",
  "version": "1.0.0",
  "description": "Aplicación web para extraer la paleta de colores de las imagenes",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsc -w & vite",
    "build": "rimraf dist && vite build",
    "start": "vite preview"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^4.9.4",
    "vite": "^4.0.0"
  }
}
